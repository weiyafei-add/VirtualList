import React from "react";
import ReactDOM from "react-dom";
// import App from "./FixedHeight";
import App from './RandomHeight';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
